<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <a
        class="bg-red-500 rounded-md px-3 py-2 text-sml mb-5 inline-block"
        href="/logout"
        >Log Out</a
      >
      <h1 class="text-3xl">
        <span class="font-light">Hello</span>, <%= user.name %>
      </h1>
      <h5 class="mb-2 mt-5">You can create a new post.</h5>
      <form action="/post" method="post">
        <textarea
          name="content"
          class="w-1/3 h-40 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 outline-none"
          placeholder="What's in your mind."
        ></textarea>
        <input
          type="submit"
          value="Create Post"
          class="px-3 py-2 bg-blue-500 block rounded-md mt-2 w-40 text-sml"
        />
      </form>
      <div class="posts mt-10">
        <h3 class="text-zinc-400">Your Posts</h3>
        <div class="postComtainer mt-5">
          <% user.post.reverse().forEach(post => { %>
          <div
            class="post w-1/3 p-5 border-2 border-zinc-200 bg-zinc-800 rounded-md md-2"
          >
            <h1 class="text-blue-500 mb-2">@<%= user.userName %></h1>
            <p class="text-sml tracking-tighter"><%= post.content %></p>
            <div class="btns flex mt-5 gap-4">
              <a href="" class="text-blue-500">Like</a>
              <a href="" class="text-zinc-300">Edit</a>
            </div>
          </div>

          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
